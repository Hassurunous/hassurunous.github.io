function onload() {
    var subtitles = document.getElementsByClassName("boxsubtitle");
    console.log(JSON.stringify(subtitles));
    var boxes = [];
    var titles = [];
    for (var i = 0; i < subtitles.length; i++) {
        //boxes.append(subtitles[i].parentNode);
    }
    for (var i = 0; i < boxes.length; i++) {

    }
}
